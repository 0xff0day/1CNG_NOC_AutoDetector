session:
  mode: exec

commands:
  normal:
    cpu: "show cpu"
    memory: "show memory"
    interfaces: "show interfaces"
    interface_errors: "show interfaces errors"
    routing: "show iproute"
    logs: "show log"
    uptime: "show uptime"
    temperature: "show temperature"
  deep_audit:
    config: "show config"
    version: "show version"
