session:
  mode: exec
commands:
  normal:
    cpu: "top -bn1 | grep 'Cpu(s)'"
    memory: "free -m"
    disk: "df -h"
    load: "uptime"
    interfaces: "ip -s link"
    logs: "journalctl -p err..alert -n 50 --no-pager 2>/dev/null || cat /var/log/syslog | tail -100"
    uptime: "uptime"
    processes: "ps aux | wc -l"
  deep_audit:
    services: "systemctl --failed --no-pager"
    dmesg: "dmesg -T | tail -n 100"
